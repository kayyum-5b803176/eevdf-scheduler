<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/hwKeyOptionToolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/white"
        android:elevation="4dp"
        app:titleTextColor="@color/textPrimary" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="8dp"
                android:text="Action on timer expiry"
                android:textSize="13sp"
                android:textColor="#757575" />

            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardCornerRadius="12dp"
                app:cardElevation="2dp"
                app:cardBackgroundColor="@android:color/white">

                <RadioGroup
                    android:id="@+id/radioGroupHwKeyOption"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="16dp" />

            </androidx.cardview.widget.CardView>

            <TextView
                android:id="@+id/tvHwKeyOptionNote"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Stop dismisses the alarm. Stop and Start dismisses the alarm and immediately restarts the same task's timer from a full slice. These keys act only while a timer-expiry alarm is ringing."
                android:textSize="12sp"
                android:textColor="#9E9E9E"
                android:lineSpacingMultiplier="1.3" />

            <TextView
                android:id="@+id/tvHwKeyPowerWarning"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:visibility="gone"
                android:text="Note: Android always turns the screen off when Power is pressed and apps can't prevent that. This action runs via screen-off detection (like the Clock app), so for Stop the alarm is dismissed as the screen turns off; for Stop and Start the app wakes the screen and restarts the task. Volume Up / Volume Down act instantly without the screen flicker."
                android:textSize="12sp"
                android:textColor="#9E9E9E"
                android:lineSpacingMultiplier="1.3" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
