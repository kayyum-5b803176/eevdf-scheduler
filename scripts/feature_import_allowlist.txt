# BASELINE of cross-feature imports.
#
# Each line is technical debt. The guard blocks any edge NOT listed here, so
# the graph can only get better. When an edge is removed, DELETE its line —
# never add one to make a build go green.
#
# History:
#   v4.3.0  15 edges  (baseline when the guard was introduced)
#   v4.4.0   5 edges  (Phase 2a: relocated misfiled code + AppRoutes)
#   v4.5.0   1 edge   (Phase 2b: AlarmController / OverlayController contracts)
#
# THE LAST ONE:
#
#   backup -> task    DataBackupActivity calls TaskViewModel.prepareForDbExport()
#                     and .prepareForDbImport(). Each is pauseTimer() +
#                     clear-current-task + a WAL checkpoint/close.
#
# Deliberately left alone. Extracting it needs a TimerSessionController that can
# stop a running timer from outside the ViewModel, and getting that wrong risks
# a half-written database during a restore — the one operation where a bug
# destroys user data. It gets done with the MainActivity/TaskViewModel split,
# where the timer lifecycle is being reworked anyway.

backup -> task
