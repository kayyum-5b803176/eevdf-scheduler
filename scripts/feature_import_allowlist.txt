# BASELINE of cross-feature imports.
#
# Each line is technical debt. The guard blocks any edge NOT listed here, so
# the graph can only get better. When an edge is removed, DELETE its line —
# never add one to make a build go green.
#
# History:
#   v4.3.0  15 edges  (baseline when the guard was introduced)
#   v4.4.0   5 edges  (Phase 2a: relocated misfiled shared code + AppRoutes)
#
# The five that remain are all SERVICE CONTROL, not misfiling — one feature
# starting, stopping or reading the state of another feature's foreground
# service. Removing them needs an abstraction (Phase 2b):
#
#   task -> alarm        TaskViewModel/TaskNoticeStateMachine drive
#                        AlarmForegroundService + AlarmScheduler + AlarmState
#   task -> autoswitch   MainActivity starts/stops BubbleOverlayService
#   autoswitch -> alarm  CallSwitchService stops the alarm on call start
#   autoswitch -> task   autoswitch services observe TaskViewModel
#   backup -> task       DataBackupActivity reads TaskViewModel
#
# Planned fix: an `AlarmController` / `OverlayController` interface in :data or
# a new :core:ui module, implemented in :app, injected via Hilt. Callers then
# depend on the contract instead of the service class.

autoswitch -> alarm
autoswitch -> task
backup -> task
task -> alarm
task -> autoswitch
