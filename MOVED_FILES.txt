Phase 7 (v5.12.0): promoted feature/ to a real Gradle module, :feature.
This is the largest single diff in the refactor (~170 files touched) — this
manifest describes it at the directory level rather than itemizing every file.

═══════════════════════════════════════════════════════════════════════════
DELETE THESE OLD PATHS ENTIRELY when applying this diff — their content now
lives in feature/ (included in this zip) or was pruned as described below.
═══════════════════════════════════════════════════════════════════════════

  app/src/main/kotlin/com/eevdf/app/feature/          (all 7 subfeatures, 69 files)
    -> feature/src/main/<subfeature>/kotlin/com/eevdf/feature/<subfeature>/
       package com.eevdf.app.feature.X -> com.eevdf.feature.X

  app/src/main/kotlin/com/eevdf/app/ui/                (5 files — NavCardView,
    DropdownCardView, ToggleCardView, ValueCardView, ModelDiagramView)
    -> feature/src/main/ui/kotlin/com/eevdf/feature/ui/
       package com.eevdf.app.ui -> com.eevdf.feature.ui

  app/src/main/res/layout/    (all 24 layouts + dialog)  -> feature/*/res/layout/
  app/src/main/res/menu/      (main_menu.xml)             -> feature/task/res/menu/
  app/src/main/res/drawable/  (24 of 26 — all except       -> feature/*/res/drawable/
                                ic_launcher_background.xml,
                                ic_launcher_foreground.xml, which STAY in :app)
  app/src/main/res/values/colors.xml                       -> feature/ui/res/values/
  app/src/main/res/values/dimens.xml                       -> feature/ui/res/values/
  app/src/main/res/values/themes.xml                       -> feature/ui/res/values/
  app/src/main/res/values-night/colors.xml                 -> feature/ui/res/values-night/

  See below for the exact per-feature drawable/layout mapping if you need it —
  everything not listed as "stays in :app" moved to the feature that
  exclusively consumes it (traced via grep, one owner each, no ambiguity):

    feature/task:       activity_main, activity_add_task, dialog_group_picker,
                         item_task, item_group_picker_*, section_add_task_*
                         (11 files), menu_action_*, main_menu; drawables:
                         baseline_delete_24, bg_dl_badge, bg_edit_text,
                         bg_group_picker_button, bg_rt_badge, ic_check_modern,
                         ic_chevron_down_modern, ic_close_modern, ic_reset_timer,
                         ic_search_modern, ic_sync, ic_undo_24,
                         outline_arrow_circle_down_24, outline_autopause_24,
                         outline_check_circle_24, outline_play_arrow_24,
                         outline_rotate_left_24, outline_timer_play_24
    feature/alarm:       activity_alarm; drawable ic_notification
    feature/autoswitch:  activity_auto_switch, overlay_bubble; drawable
                         bubble_background
    feature/backup:      activity_data_backup; drawable bg_warning_notice
    feature/settings:    activity_button_action, activity_color_matrix,
                         activity_display_settings, activity_hardware_key_*,
                         activity_layout_demo, activity_profile_settings,
                         activity_settings, activity_sound_vibration
    feature/stats:       activity_stats, fragment_stats_*
    feature/sync:        activity_multiuser_sync; drawable badge_bg
    feature/ui:          view_*_internal.xml (4), drawable ic_sync_dot +
                         outline_skip_next_24 (genuinely used by 2+ features)

  app/src/main/res/values/strings.xml (16 entries, ALL unwired — none
    referenced anywhere in code except app_name via the manifest)
    -> 14 entries -> feature/task/res/values/strings.xml
    -> 1  entry  (about_eevdf, lowest-confidence placement, no About
                  screen currently exists) -> feature/settings/res/values/strings.xml
    -> app_name stays in app/src/main/res/values/strings.xml (this zip's
       version has only that one string now)

═══════════════════════════════════════════════════════════════════════════
NEW — everything under feature/ in this zip is new (module didn't exist before)
═══════════════════════════════════════════════════════════════════════════
  feature/build.gradle.kts
  feature/src/main/AndroidManifest.xml   (all Activities/Services/Receivers,
    merges into :app's <application> tag at build time)
  feature/src/main/{task,alarm,autoswitch,backup,settings,stats,sync,ui}/
    {kotlin,res}/  — full contents in this zip

═══════════════════════════════════════════════════════════════════════════
MODIFIED
═══════════════════════════════════════════════════════════════════════════
  settings.gradle.kts       — include(...) gains ":feature"
  app/build.gradle.kts      — dependencies gains implementation(project(":feature"));
                               versionName 5.11.0 -> 5.12.0 (versionCode unchanged)
  build.gradle.kts          — detekt source set gains all 8 feature/src/main/*/kotlin dirs
  app/src/main/AndroidManifest.xml — trimmed to permissions + bare <application>
                               shell; all components moved to feature/'s manifest
  app/src/main/res/values/strings.xml — trimmed to just app_name
  contract/.../nav/AppRoutes.kt        — constants updated to com.eevdf.feature.*
  contract/.../control/AlarmController.kt  — stale doc-comment package mention fixed
  contract/.../control/AlarmActions.kt     — stale doc-comment package mention fixed
  app/src/test/.../contract/AppRoutesTest.kt — prefix filter updated to "com.eevdf.feature."
  scripts/check_architecture.sh — feature-isolation scan rewritten for the new
                               physical layout (feature/src/main/<name>/kotlin/,
                               excludes ui/ from the isolation check since every
                               feature is expected to import it); god-file and
                               long-function scans now include feature/ and contract/
  CODEOWNERS                — added /feature/build.gradle.kts and
                               /feature/src/main/AndroidManifest.xml as shared surfaces
  docs/ADDING_A_FEATURE.md  — checklist updated for the new physical layout and
                               the "add your subfeature to feature/build.gradle.kts" step
  ARCHITECTURE.md           — :feature flipped to real module; Phase 7 marked done
                               (out of original order — see the doc for why);
                               stale core.nav mention from Phase 4 fixed; Phase 6/9
                               paths updated; explicit note that the allowlist was
                               NOT deleted (per-feature isolation still script-enforced,
                               only app->feature direction is now compiler-enforced)

═══════════════════════════════════════════════════════════════════════════
NOT done / deliberately deferred
═══════════════════════════════════════════════════════════════════════════
  - :designsystem as its own module for feature/ui/ — considered, deferred until
    :contract and :feature are both build-verified (see ARCHITECTURE.md Phase 7)
  - scripts/feature_import_allowlist.txt — untouched, still enforces per-feature
    isolation within :feature (one module ≠ per-feature compile isolation)
  - app/core/{prefs,media,notification,signals} — untouched, Phase 5's job
