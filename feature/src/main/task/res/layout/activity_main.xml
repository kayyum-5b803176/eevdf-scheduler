<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screenBackground">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/headerBackground"
            app:titleTextColor="@android:color/white"
            app:popupTheme="@style/ThemeOverlay.MaterialComponents.Light" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/headerBackground"
            app:tabIndicatorColor="@android:color/white"
            app:tabTextColor="@color/app_white_67"
            app:tabSelectedTextColor="@android:color/white" />

        <!-- Stats bar -->
        <LinearLayout
            android:id="@+id/statsBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/statsBarBackground"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="@dimen/app_card_padding_md"
            android:paddingVertical="@dimen/app_spacing_md">

            <!-- Left column: two stats lines -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvStats"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Active: 0  |  Done: 0  |  Weight: 0.0"
                    android:textColor="@color/app_white_80"
                    android:textSize="@dimen/app_text_size_sm" />

                <TextView
                    android:id="@+id/tvFairness"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Fairness: 100%  |  Avg VRT: 0.00"
                    android:textColor="@color/app_white_67"
                    android:textSize="@dimen/app_text_size_sm" />
            </LinearLayout>

            <!-- Right: queue rank of current running task -->
            <TextView
                android:id="@+id/tvScheduleRank"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="#1"
                android:textColor="@android:color/white"
                android:textSize="@dimen/app_text_size_lg"
                android:fontFamily="sans-serif-medium"
                android:layout_marginStart="@dimen/app_spacing_lg"
                android:visibility="gone" />

        </LinearLayout>

    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">



        <!-- ════════════════════════════════════════════════════════════════
             TIMER CARD — two fully-styled content blocks, zero shared styling
             ────────────────────────────────────────────────────────────────
             CLEAN/STABLE APPROACH:

             The countdown view and the expired/alarm view are TWO SEPARATE,
             SELF-CONTAINED blocks. Each owns its own views with ALL styling
             (text size, typeface, color) declared HERE in XML and NEVER mutated
             from Kotlin. renderTimerCard() only:
                • toggles which block is visible
                • sets the card background color
                • writes TEXT CONTENT (never size/style/color)

             Because no view is shared between the two visual identities, a style
             tweak in one state can never leak into the other — eliminating the
             "fix Stop, break Start" class of bugs entirely.

             Both blocks have identical structure (a header row + a button row,
             same paddings/margins/text sizes), so their measured heights match
             and switching blocks does not resize the card or move the list.
             ════════════════════════════════════════════════════════════════ -->
        <com.google.android.material.card.MaterialCardView
        style="@style/App.Card"
            android:id="@+id/cardTimer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:layout_marginStart="@dimen/app_card_gap"
            android:layout_marginEnd="@dimen/app_card_gap"
            android:layout_marginTop="@dimen/app_card_gap"
            android:layout_marginBottom="@dimen/app_card_gap">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <!-- ░░░░░░░░░░░░░░░░ COUNTDOWN BLOCK ░░░░░░░░░░░░░░░░ -->
                <LinearLayout
                    android:id="@+id/layoutTimerContent"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="@dimen/app_card_padding_md"
                    android:clipChildren="false"
                    android:clipToPadding="false">

                    <!-- Header: name + priority left, countdown right -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <LinearLayout
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:orientation="vertical">

                            <TextView
                                android:id="@+id/tvCurrentTaskName"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="Task Name"
                                android:textColor="@android:color/white"
                                android:textSize="@dimen/app_text_size_lg"
                                android:fontFamily="sans-serif-medium"
                                android:maxLines="1"
                                android:ellipsize="end" />

                            <TextView
                                android:id="@+id/tvTimerPriority"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="Priority 5 · General"
                                android:textColor="@color/app_white_67"
                                android:textSize="@dimen/app_text_size_sm" />
                        </LinearLayout>

                        <TextView
                            android:id="@+id/tvTimerDisplay"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="25:00"
                            android:textColor="@android:color/white"
                            android:textSize="@dimen/app_text_size_cl"
                            android:fontFamily="monospace"
                            android:layout_marginStart="@dimen/app_spacing_xl" />
                    </LinearLayout>

                    <!-- Button row + phase underline overlay -->
                    <FrameLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="@dimen/app_spacing_lg"
                        android:clipChildren="false"
                        android:clipToPadding="false">

                        <LinearLayout
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:orientation="horizontal"
                            android:gravity="center">

                            <com.google.android.material.button.MaterialButton
                                android:id="@+id/btnScheduleNext"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="Next"
                                android:textColor="@android:color/white"
                                app:backgroundTint="@android:color/transparent"
                                android:layout_marginEnd="@dimen/app_spacing_md"
                                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                                app:strokeColor="@android:color/white" />

                            <com.google.android.material.button.MaterialButton
                                android:id="@+id/btnStartPause"
                                android:layout_width="0dp"
                                android:layout_height="wrap_content"
                                android:layout_weight="1"
                                android:text="Start"
                                app:backgroundTint="@color/timerGreen"
                                android:textColor="@android:color/white"
                                android:layout_marginHorizontal="@dimen/app_spacing_sm" />

                            <com.google.android.material.button.MaterialButton
                                android:id="@+id/btnInt"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="INT-A"
                                android:textColor="@color/app_text_label"
                                android:fontFamily="sans-serif-medium"
                                android:layout_marginStart="@dimen/app_spacing_md"
                                app:backgroundTint="@color/intButtonBackground" />
                        </LinearLayout>

                        <View
                            android:id="@+id/viewPhaseStatus"
                            android:layout_width="match_parent"
                            android:layout_height="5dp"
                            android:layout_gravity="bottom"
                            android:translationY="5dp"
                            android:background="@color/app_green_nd"
                            android:visibility="gone" />

                    </FrameLayout>
                </LinearLayout>

                <!-- ░░░░░░░░░░░░░░░░ EXPIRED / ALARM BLOCK ░░░░░░░░░░░░░░░░ -->
                <LinearLayout
                    android:id="@+id/layoutAlarmContent"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="@dimen/app_card_padding_md"
                    android:visibility="gone">

                    <!-- Header: name + subtitle left, elapsed right -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <LinearLayout
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:orientation="vertical">

                            <TextView
                                android:id="@+id/tvAlarmTaskName"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="Task Name"
                                android:textColor="@android:color/white"
                                android:textSize="@dimen/app_text_size_lg"
                                android:fontFamily="sans-serif-medium"
                                android:maxLines="1"
                                android:ellipsize="end" />

                            <TextView
                                android:id="@+id/tvAlarmSubtitle"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="Time slice complete · tap Stop to dismiss"
                                android:textColor="@color/app_red_lt"
                                android:textSize="@dimen/app_text_size_sm"
                                android:maxLines="1"
                                android:ellipsize="end" />
                        </LinearLayout>

                        <TextView
                            android:id="@+id/tvAlarmElapsed"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="0:00"
                            android:textColor="@android:color/white"
                            android:textSize="@dimen/app_text_size_cl"
                            android:fontFamily="monospace"
                            android:layout_marginStart="@dimen/app_spacing_xl" />
                    </LinearLayout>

                    <!-- Stop button row: full width, matches countdown button-row height -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center"
                        android:layout_marginTop="@dimen/app_spacing_lg">

                        <com.google.android.material.button.MaterialButton
                            android:id="@+id/btnStopAlarm"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:text="Stop"
                            android:textColor="@color/stopButtonText"
                            android:textSize="@dimen/app_text_size_lg"
                            android:fontFamily="sans-serif-medium"
                            app:backgroundTint="@color/stopButtonBackground"
                            app:cornerRadius="8dp" />
                    </LinearLayout>
                </LinearLayout>

            </FrameLayout>
        </com.google.android.material.card.MaterialCardView>
        <!-- Empty state -->
        <LinearLayout
            android:id="@+id/emptyView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="No tasks yet"
                android:textSize="@dimen/app_text_size_xl"
                android:fontFamily="sans-serif-medium"
                android:textColor="@color/app_text_body"
                android:layout_marginTop="@dimen/app_spacing_xl" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Tap + to add your first task"
                android:textSize="@dimen/app_text_size_md"
                android:textColor="@color/app_text_caption"
                android:layout_marginTop="@dimen/app_spacing_md" />
        </LinearLayout>

        <!-- Drill-down breadcrumb bar (Links feature). Hidden in flat-outline
             mode and whenever the current tab's drill stack is empty. Tapping
             it pops exactly one frame — for a symlink hop this returns to the
             symlink's HOST group, never the real group's own real parent. -->
        <LinearLayout
            android:id="@+id/breadcrumbBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="@dimen/app_spacing_sm"
            android:paddingEnd="@dimen/app_spacing_md"
            android:paddingTop="@dimen/app_spacing_sm"
            android:paddingBottom="@dimen/app_spacing_sm"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackground"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivBreadcrumbBack"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:rotation="90"
                android:src="@drawable/ic_chevron_down_modern"
                android:contentDescription="Back"
                app:tint="@color/app_text_label" />

            <TextView
                android:id="@+id/tvBreadcrumbPath"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/app_spacing_sm"
                android:textSize="@dimen/app_text_size_sm"
                android:textColor="@color/app_text_caption"
                android:maxLines="1"
                android:ellipsize="start"
                android:singleLine="true" />

        </LinearLayout>

        <!-- Task list -->
        <androidx.recyclerview.widget.RecyclerView
            style="@style/App.List.Content"
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="vertical" />

    </LinearLayout>

    <!-- Quick Action FAB — sits above the Add Task FAB.
         Visible only when Quick Action is enabled in Button Action settings.
         Taps the currently active INT button (A or B) and immediately starts
         the timer. Uses layout_anchor to stack it 8dp above fabAdd. -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabQuickAction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="@dimen/app_spacing_xl"
        android:layout_marginBottom="@dimen/app_spacing_cl"
        android:src="@drawable/outline_autopause_24"
        app:backgroundTint="@color/colorPrimary"
        app:tint="@android:color/white"
        android:contentDescription="Quick Action"
        android:visibility="gone" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/app_card_padding_md"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="@color/colorAccent"
        app:tint="@android:color/white"
        android:contentDescription="Add Task" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
