<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/app_size_row"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="?attr/selectableItemBackground">

    <TextView
        android:id="@+id/tvPickerEntryName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="15sp"
        android:textColor="#212121"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- Checkmark shown when this item is the current selection -->
    <ImageView
        android:id="@+id/ivPickerCheck"
        android:layout_width="@dimen/app_icon_sm"
        android:layout_height="@dimen/app_icon_sm"
        android:src="@drawable/ic_check_modern"
        android:tint="@color/colorPrimary"
        android:visibility="invisible"
        android:contentDescription="Selected" />

</LinearLayout>
