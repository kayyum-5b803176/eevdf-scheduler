<?xml version="1.0" encoding="utf-8"?>
<!--
  Per-capability manifest fragment. AGP's manifest merger combines these into
  the single application manifest at build time, so each capability declares
  the Android components it owns instead of all of them living in one shared
  file that every capability had to edit.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.eevdf.capabilities.tasklistscreen.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
